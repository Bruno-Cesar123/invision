import styled from "styled-components";

export const Container = styled.button`
  margin-top: 40px;
  height: 45px;
  width: 263px;

  display: flex;
  align-items: center;
  justify-content: center;

  border: none;
  border-radius: 100px;
  background: var(--white);
  box-shadow: 0px 3px 6px var(--gray-100);
  color: var(--black);
  font-size: 18px;

  img {
    margin-right: 20px;
  }

`;